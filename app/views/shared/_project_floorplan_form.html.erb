<div class="col-md-6 col-md-offset-3">
    <h1>Add a project!</h1>
    <%= form_for setup_project(@project), :html => {:multipart => true } do |f| %>
      
        <p>Project Name</p>
        <%= f.text_field :name%>
      
        <%= f.fields_for :floorplans, @floorplan do |ff| %>
	        <p>Floorplans</p>
	        <%= ff.file_field :blueprint, multiple: true %>
	    <% end %>
          
      	<%= f.submit "Submit", class: "btn btn-primary" %>
    <% end %>
</div>